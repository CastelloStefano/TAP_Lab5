
using System;
using System.Collections;
using System.Collections.Generic;
using System.Linq;

namespace Macro
{
    public class Macro
    {
        public static T MacroExpansion<T>(IEnumerable<T> sequence, T value, IEnumerable<T> newValues)
        {
            // Controllo dei tipi
           // if()
           // foreach (var x1 in newValues.ToList())
              //  sequence.ToList().Insert(1,x1);

            // ricerca

            // sostituzione



            return (T) value;
        }
    }
}
